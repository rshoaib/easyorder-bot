/**
 * PricingAnnouncementBanner
 *
 * Previously announced an upcoming $10/mo Pro tier inside the admin
 * dashboard. OrderViaChat is now 100% free for all stores, so there is
 * no pricing announcement to make. Component reduced to a no-op.
 */
export default function PricingAnnouncementBanner() {
    return null;
}
